# ~/personal — root map

Shane's personal (non-venture) workspace — **the personal vault**, a private git repo (`spschleger/personal`) cloned to `~/personal` on both the M5 and the mini. Router only — each area's `AGENTS.md` is canonical for its own contents. Max (the HQ/PA agent on the mini) reads and commits here like any other agent.

**Nested repos** (own remotes, ignored by this repo): `max-personal/` (Max's personal memory + `FLEET.md`, the front door to the whole setup), `finances/weekly-budget/`, `Yujie/YesMandarin/mandarin-trial-booker/`, `Yujie/YesMandarin/content-pipeline/`. Commit inside them for their changes; commit here for everything else. Push both.

- **`finances/`** — budget app, bank-export ingestion, tax return, strategy. See `finances/AGENTS.md`.
- **`health-appearance/`** — TRT trackers, sleep-apnea hub, training/recomp. See `health-appearance/AGENTS.md`.
- **`Yujie/`** — partner-related: YesMandarin bookkeeping, private health workup. See `Yujie/AGENTS.md`.
- **`Work/`** — resumes, tickets/certs, employer docs. Documents only.
- **`family-history/`** — Schleger + Jacklin dossiers, sources, the Vercel site.
- **`tools/`** — small utilities (html2pdf).

**`MACHINE.md`** — the machine rebuild manifest: where every category of state lives, identity inventory, restore paths. Keep a copy in `~/Documents/` (iCloud). Update after structural changes.

**`CLAUDE-STACK.md`** — how the Claude layers are wired: context layers (constitution/memory/AGENTS.md/skills), capability layers (shell/MCP/plugins), global-vs-project scoping, and the where-does-a-new-thing-go decision table. Copy in `~/Documents/` too.

Ventures live in `~/projects/` under the spsholdco account (micko, wisper, hq = Max's business brain; everything else archived under `~/projects/_archive/`). Never write personal material into `~/projects/*`.
